node {
        stage('ansible') {
          ansiblePlaybook (
          colorized: true, 
          become: true, 
          playbook: 'main.yaml',
          inventory: 'inventaire'
 )
 }
        
}

